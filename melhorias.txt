# INTEGRAÇÃO CONTROLADA — CATÁLOGO GLOBAL + ORIGIN/MAIN

A auditoria de divergência foi concluída.

Estado atual conhecido:

```text id="6f8q1v"
HEAD local:
18748f7eaa5cf715e0ade18e44c6bd96fcbc3cfe

origin/main:
f6d49436194204ecc11cd27fc832a87a7b0bf919

merge-base:
ed01191f0e03a2d39ab259b57eff43f33e8dde70
```

O working tree está limpa.

O objetivo é criar uma nova branch de integração contendo:

### Tudo que existe no origin/main

incluindo:

* booking;
* disponibilidade;
* WhatsApp;
* produtos;
* reagendamento;
* trial de 30 dias;
* Mercado Pago;
* pagamentos;
* integrações;
* correções existentes;
* migrations remotas;
* alterações de UI/painel.

### MAIS o catálogo global local

incluindo:

* 20 segmentos;
* 300 templates;
* exatamente 15 templates por segmento;
* templates globais;
* cópia independente para services;
* serviços customizados;
* limites por plano;
* downgrade seguro;
* `business_segments` protegido;
* `professional_services` protegido por tenant;
* `Geral` apenas legado/técnico;
* sem sincronização template → service vivo.

---

# REGRA PRINCIPAL

Agora está AUTORIZADO executar a integração Git na branch de integração.

Porém:

## NÃO fazer push.

## NÃO executar migrations no banco remoto.

## NÃO fazer force-push.

## NÃO alterar `origin/main`.

## NÃO apagar branches.

## NÃO usar reset destrutivo.

## NÃO escolher automaticamente "ours" ou "theirs" nos conflitos.

A integração deve ser feita preservando funcionalmente os dois lados.

---

# ETAPA 1 — Criar branch de integração

Parta de:

```text id="2x3q5c"
origin/main
```

Crie:

```bash
git switch -c integrate-global-catalog origin/main
```

Antes de continuar, confirme:

```bash
git status
git branch --show-current
git rev-parse HEAD
```

A branch deve apontar inicialmente para:

```text
f6d49436194204ecc11cd27fc832a87a7b0bf919
```

---

# ETAPA 2 — Verificar teste de assinatura

Antes de aplicar `2bf7717`, verifique se já existe:

```text id="z7y8j2"
tests/subscription-plans.test.ts
```

Se já existir e cobrir adequadamente os casos adicionados por `2bf7717`, NÃO aplique `2bf7717`.

Se não existir, aplique:

```bash
git cherry-pick 2bf7717
```

Se houver conflito, pare e analise antes de continuar.

---

# ETAPA 3 — NÃO aplicar commits duplicados

NÃO aplicar:

```text id="3gk5x4"
1bf8c2b
1ac8206
```

Eles já possuem equivalentes funcionais no remoto:

```text id="c18f07a"
c18f07a ≈ 1bf8c2b

479b409 ≈ 1ac8206
```

Não duplicar essas alterações.

---

# ETAPA 4 — Integrar o catálogo

Aplicar nesta ordem:

```bash
git cherry-pick 0303a99
git cherry-pick 48135b9
git cherry-pick de9cfff
git cherry-pick 18748f7
```

A cadeia deve permanecer:

```text
0303a99
   ↓
48135b9
   ↓
de9cfff
   ↓
18748f7
```

---

# ETAPA 5 — RESOLUÇÃO DOS CONFLITOS

Existem seis arquivos conhecidos com conflitos:

```text id="9j4n8p"
src/lib/booking.functions.ts
src/lib/booking.server.ts
src/lib/subscription.functions.ts
src/routes/$slug.tsx
src/routes/_authenticated/app.assinatura.tsx
src/routes/_authenticated/app.servicos.tsx
```

NÃO resolva usando:

```bash
git checkout --ours
git checkout --theirs
```

nem equivalente automático.

Resolver FUNCIONALMENTE.

---

## 5.1 booking.functions.ts

Preservar o comportamento remoto de:

* booking;
* produtos;
* políticas;
* presença;
* confirmação;
* WhatsApp;
* payloads existentes.

Adicionar/preservar do catálogo:

* informação de segmento;
* `segment_id`;
* `segments`;
* compatibilidade com catálogo sem segmento.

A alteração de catálogo deve ser ADITIVA.

Não remover nenhum campo ou fluxo remoto.

---

## 5.2 booking.server.ts

Preservar integralmente do remoto:

* disponibilidade;
* horários;
* conflitos;
* profissionais;
* serviços;
* produtos;
* appointments;
* regras de atendimento paralelo;
* reagendamento;
* demais regras recentes.

Adicionar do catálogo:

* segmento global;
* `segment_id`;
* associação business → segment;
* agrupamento/metadata necessários ao catálogo;
* ocultação de `Geral` como categoria comercial.

Não alterar o cálculo de disponibilidade.

Não alterar duração.

Não alterar conflitos.

Não alterar regras de double booking.

---

## 5.3 subscription.functions.ts

Este é um arquivo CRÍTICO.

Preservar do remoto:

* Mercado Pago;
* trial;
* criação de assinatura;
* checkout;
* entitlements;
* pagamentos;
* regras existentes.

Preservar do catálogo:

* Basic = máximo 1 segmento;
* Medium = máximo 2;
* Unlimited = ilimitado;
* Trial = ilimitado;
* bloqueio seguro de downgrade.

Também preservar:

* Basic anual = 11 meses;
* Medium anual = 10 meses;
* Unlimited anual = 9 meses;
* data segura de `auto_recurring.start_date`.

NÃO substituir o arquivo inteiro.

---

# 5.4 billing.server.ts

Se houver qualquer conflito ou alteração inesperada neste arquivo:

```text id="1v7d2z"
src/lib/billing.server.ts
```

PARAR imediatamente e reportar.

A versão final deve manter:

```text id="s5j2q7"
platformSetting
resolveProvider
graceDays
loadSubscriptionByBusiness
loadSubscriptionByProviderId
loadPlanById
businessEntitlements
assertAcceptsBookings
```

Não substituir o arquivo inteiro.

A diferença conhecida entre local/remoto é somente formatação de `resolveProvider`.

---

# 5.5 $slug.tsx

Preservar do remoto:

* fluxo público;
* disponibilidade;
* produtos;
* reagendamento;
* cancelamento;
* presença;
* WhatsApp;
* políticas;
* links seguros.

Adicionar do catálogo:

* agrupamento por segmento;
* `segment_id`;
* informações de segmento necessárias para apresentação.

A seleção dos serviços deve continuar utilizando os IDs reais existentes.

Não alterar o payload de booking sem necessidade.

Não alterar a lógica de disponibilidade.

---

# 5.6 app.assinatura.tsx

Preservar:

* checkout;
* PIX;
* Mercado Pago;
* mensal/anual;
* estado da assinatura.

Adicionar/preservar:

```text
Medium = 🔥 MAIS ASSINADO
Unlimited = 👑 MAIS COMPLETO
```

E:

```text
Basic = 1 segmento
Medium = até 2 segmentos
Unlimited = ilimitado
Trial = ilimitado
```

Serviços continuam ilimitados em todos os planos.

---

# 5.7 app.servicos.tsx

Este é um dos conflitos mais importantes.

Preservar do remoto:

* serviços;
* preço;
* duração;
* ativação/desativação;
* edição;
* exclusão;
* `allows_parallel`;
* funcionalidades existentes.

Preservar do catálogo:

* segmentos;
* templates;
* seleção de segmentos;
* cópia de templates;
* serviços customizados;
* associação de profissionais;
* remoção de profissionais;
* limites;
* `Geral` como legado/técnico.

O resultado precisa conter simultaneamente:

```text
segment_id
allows_parallel
professional associations
catalog templates
custom services
```

Não substituir a tela inteira por uma das versões.

---

# ETAPA 6 — Migrations

Após a integração dos arquivos, confirme que todas estas migrations estão presentes:

```text id="8x0d5v"
20260906140000_phase2a_instagram.sql
20260906150000_phase0_booking_integrity.sql
20260906170000_phase3_whatsapp_outbox.sql
20260906190000_trial_30_days_all_features.sql
20260906195900_add_rescheduled_status.sql
20260906200000_products_and_reschedule_atomic.sql
20260906210000_update_annual_plan_discounts.sql
20260906220000_fix_basic_annual_discount.sql
20260907000000_segments_service_catalog.sql
20260907001000_public_catalog_segments.sql
20260907002000_catalog_structural_fix.sql
20260907003000_complete_global_catalog.sql
20260907004000_catalog_integrity_blockers.sql
```

NÃO execute nenhuma migration.

Somente validar presença, ordem e conteúdo.

---

# ETAPA 7 — Validar catálogo

Depois da integração, verificar estaticamente:

* 20 segmentos;
* 300 templates;
* 15 por segmento;
* nomes corretos;
* durações corretas;
* `segments` global;
* `service_templates` global;
* cópia independente;
* ausência de sincronização;
* `Geral` legado;
* nenhum fallback automático;
* limites por plano;
* downgrade;
* triggers de integridade.

---

# ETAPA 8 — Validar funcionalidades remotas

Verificar que continuam presentes:

* booking;
* disponibilidade;
* WhatsApp;
* produtos;
* reagendamento;
* trial;
* Instagram;
* Mercado Pago;
* pagamentos;
* configurações;
* painel.

---

# ETAPA 9 — Testes

Executar:

```bash
npm test
npx tsc --noEmit
npm run lint
npm run build
git diff --check
```

Se existirem testes específicos, executar também:

```text
tests/segments.test.ts
tests/catalog.test.ts
tests/catalog-integrity.test.ts
tests/subscription-plans.test.ts
tests/availability.test.ts
tests/whatsapp.test.ts
tests/instagram.test.ts
```

Se algum teste falhar:

1. identificar causa;
2. corrigir;
3. executar novamente;
4. não ignorar falha.

---

# ETAPA 10 — Auditoria final do resultado

Verificar:

```bash
git status
git log --oneline --decorate --graph -30
git diff origin/main...HEAD --stat
```

Também verificar:

```bash
git diff --check
```

Confirmar que:

* working tree limpa;
* nenhum arquivo crítico foi perdido;
* nenhum arquivo remoto importante desapareceu;
* migrations locais estão presentes;
* migrations remotas estão presentes;
* testes estão presentes.

---

# ETAPA 11 — NÃO criar commit final ainda

Depois que tudo estiver funcionando:

NÃO criar ainda um commit manual adicional.

NÃO fazer push.

NÃO fazer merge.

NÃO abrir PR.

Pare e apresente o relatório.

---

# RELATÓRIO FINAL OBRIGATÓRIO

Entregue:

## 1. Branch criada

## 2. Commits aplicados

## 3. Commits não aplicados e motivo

## 4. Conflitos encontrados

Para cada conflito:

* arquivo;
* conflito;
* decisão tomada;
* funcionalidades preservadas do remoto;
* funcionalidades preservadas do catálogo.

## 5. Billing

Confirme explicitamente:

* `platformSetting` presente;
* Mercado Pago preservado;
* trial preservado;
* entitlements preservados.

## 6. Booking

Confirme:

* disponibilidade;
* produtos;
* reagendamento;
* WhatsApp;
* booking;
* conflitos/double booking.

## 7. Catálogo

Confirme:

* 20 segmentos;
* 300 templates;
* 15 por segmento;
* cópia independente;
* `Geral` legado;
* limites;
* downgrade;
* integridade multi-tenant.

## 8. Migrations

Liste todas as 13 migrations na ordem final.

## 9. Testes

Informe resultado de:

```text
npm test
npx tsc --noEmit
npm run lint
npm run build
git diff --check
```

## 10. Git final

Informe:

* branch atual;
* HEAD;
* origin/main;
* working tree;
* commits à frente/atrás.

## 11. Riscos restantes

Classifique:

🔴 alto
🟠 médio
🟢 baixo

## 12. Veredito

Escolha:

🟢 INTEGRAÇÃO CONCLUÍDA — PRONTA PARA AUDITORIA FINAL

ou

🟡 INTEGRAÇÃO CONCLUÍDA — REQUER CORREÇÕES

ou

🔴 INTEGRAÇÃO NÃO CONCLUÍDA

---

### REGRA FINAL

O objetivo desta etapa é somente chegar a uma branch de integração local funcional.

NÃO fazer:

* push;
* force-push;
* merge em main;
* PR;
* migration remota;
* publicação.

Somente depois de minha revisão do relatório será autorizada qualquer publicação.
