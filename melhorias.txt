## AUDITORIA GIT — NÃO ALTERAR O REPOSITÓRIO

Estamos no commit local:

`18748f7eaa5cf715e0ade18e44c6bd96fcbc3cfe`

O `origin/main` está em:

`f6d49436194204ecc11cd27fc832a87a7b0bf919`

O merge-base informado é:

`ed01191f0e03a2d39ab259b57eff43f33e8dde70`

O HEAD local está em **detached HEAD**.

Quero uma auditoria completa da divergência antes de decidir como integrar o trabalho.

### REGRA ABSOLUTA

NÃO executar nenhuma operação que altere o histórico ou working tree.

É PROIBIDO:

* `git merge`
* `git rebase`
* `git cherry-pick`
* `git reset`
* `git checkout` para alterar estado
* `git switch`
* `git restore`
* `git commit`
* `git push`
* `git pull`
* `git fetch`
* alterar arquivos
* aplicar migrations no banco remoto

Execute somente comandos de leitura.

---

# 1. Estado atual

Execute:

```bash
git status
git branch -a
git rev-parse HEAD
git rev-parse origin/main
git merge-base HEAD origin/main
```

Confirme se os hashes continuam exatamente:

```text
HEAD:
18748f7eaa5cf715e0ade18e44c6bd96fcbc3cfe

origin/main:
f6d49436194204ecc11cd27fc832a87a7b0bf919

merge-base:
ed01191f0e03a2d39ab259b57eff43f33e8dde70
```

---

# 2. Liste os commits dos dois lados

Execute:

```bash
git log --oneline --decorate --graph --all -50
```

Depois:

```bash
git log --oneline ed01191f0e03a2d39ab259b57eff43f33e8dde70..HEAD
```

e:

```bash
git log --oneline ed01191f0e03a2d39ab259b57eff43f33e8dde70..origin/main
```

Identifique claramente:

### COMMITS LOCAIS

Todos os commits presentes em `HEAD` depois do merge-base.

### COMMITS REMOTOS

Todos os commits presentes em `origin/main` depois do merge-base.

Para cada commit, informe:

* hash curto;
* mensagem;
* arquivos alterados;
* resumo objetivo da alteração.

---

# 3. Auditar os 6 commits locais

Para cada commit local, execute/analise:

```bash
git show --stat --oneline <HASH>
```

e:

```bash
git show --format=fuller --stat <HASH>
```

Não precisa reproduzir diffs gigantescos.

Quero saber principalmente quais arquivos foram alterados e qual funcionalidade cada commit representa.

Dê atenção especial a:

```text
src/lib/billing.server.ts
src/lib/subscription.functions.ts
src/lib/booking.server.ts
src/lib/booking.functions.ts
src/routes/_authenticated/app.servicos.tsx
src/routes/_authenticated/app.assinatura.tsx
supabase/migrations/*
tests/*
```

---

# 4. Auditar os 16 commits de origin/main

Faça a mesma análise para TODOS os commits que estão em:

```text
origin/main
```

mas não estão no HEAD local.

Para cada um informe:

* hash;
* mensagem;
* arquivos alterados;
* funcionalidade;
* se parece ser correção, feature, refactor, auditoria ou outro tipo de mudança.

---

# 5. Identificar sobreposição de arquivos

Compare os arquivos modificados pelos dois lados.

Quero uma tabela:

| Arquivo | Alterado localmente | Alterado no origin/main | Risco            |
| ------- | ------------------- | ----------------------- | ---------------- |
| arquivo | sim/não             | sim/não                 | baixo/médio/alto |

Dê prioridade máxima a:

```text
src/lib/billing.server.ts
src/lib/subscription.functions.ts
src/lib/booking.server.ts
src/lib/booking.functions.ts
src/routes/_authenticated/app.servicos.tsx
src/routes/_authenticated/app.assinatura.tsx
supabase/migrations/*
tests/*
```

---

# 6. Detectar conflitos potenciais

SEM executar merge ou rebase, use comandos somente de leitura para avaliar possíveis conflitos.

Se possível, utilize:

```bash
git merge-tree $(git merge-base HEAD origin/main) HEAD origin/main
```

Se a versão do Git não aceitar essa sintaxe, não tente alterar nada; apenas informe.

Também analise manualmente os arquivos que aparecem nos dois lados.

Quero classificar:

### 🔴 CONFLITO REAL

As duas linhas modificaram a mesma parte lógica do arquivo e provavelmente exigiriam resolução manual.

### 🟠 CONFLITO POTENCIAL

Os dois lados alteraram o mesmo arquivo, mas em regiões aparentemente independentes.

### 🟢 SEM CONFLITO RELEVANTE

Alterações independentes ou facilmente preserváveis.

---

# 7. Verificar especificamente o billing

Isso é CRÍTICO.

O arquivo:

```text
src/lib/billing.server.ts
```

foi anteriormente corrompido quando conteúdo de booking substituiu o billing.

O billing correto foi restaurado no commit:

```text
ed01191
```

Depois disso, precisamos garantir que nenhum dos 16 commits de `origin/main` tenha:

* sobrescrito `billing.server.ts`;
* removido `platformSetting`;
* alterado `resolveProvider`;
* alterado `graceDays`;
* alterado `loadSubscriptionByBusiness`;
* alterado `loadSubscriptionByProviderId`;
* alterado `loadPlanById`;
* alterado `businessEntitlements`;
* alterado `assertAcceptsBookings`;
* alterado Mercado Pago;
* alterado trial;
* alterado preços anuais.

Informe explicitamente se existe algum risco aqui.

---

# 8. Verificar especificamente o catálogo de serviços

O trabalho local recente contém:

```text
48135b9
de9cfff
18748f7
```

Esse trabalho implementou/corrigiu:

* catálogo global;
* 20 segmentos;
* 300 templates;
* 15 templates por segmento;
* cópia independente de templates para services;
* ausência de sincronização template → service;
* `Geral` apenas como legado/técnico;
* limites de segmentos por plano;
* downgrade seguro;
* associação de profissionais;
* integridade multi-tenant;
* trigger para `business_segments`;
* trigger para `professional_services`.

Verifique se algum commit de `origin/main` alterou as mesmas áreas.

Especial atenção a:

```text
supabase/migrations/20260907002000_catalog_structural_fix.sql
supabase/migrations/20260907003000_complete_global_catalog.sql
supabase/migrations/20260907004000_catalog_integrity_blockers.sql

src/lib/subscription.functions.ts
src/routes/_authenticated/app.servicos.tsx
tests/catalog.test.ts
tests/catalog-integrity.test.ts
```

Informe qualquer risco de perder ou sobrescrever esse trabalho.

---

# 9. Verificar migrations

Liste todas as migrations que existem em cada lado desde o merge-base.

Compare:

```text
local HEAD
versus
origin/main
```

Identifique:

* migrations existentes apenas localmente;
* migrations existentes apenas no remoto;
* migrations que alteram as mesmas tabelas;
* migrations potencialmente dependentes umas das outras;
* qualquer risco de executar uma migration na ordem errada.

Não execute nenhuma migration.

---

# 10. Verificar se os commits locais são dependentes entre si

Determine se:

```text
48135b9
de9cfff
18748f7
```

dependem uns dos outros.

Quero saber se eles precisam permanecer juntos como uma cadeia ou se algum deles pode ser integrado isoladamente.

Também identifique se algum dos 16 commits remotos depende de alterações feitas depois do merge-base.

---

# 11. Verificar conteúdo efetivo

Além do histórico, compare o estado final dos arquivos importantes entre:

```text
HEAD
origin/main
```

Use comandos de leitura como:

```bash
git diff --stat HEAD origin/main
```

e, para arquivos críticos:

```bash
git diff HEAD origin/main -- <arquivo>
```

Não precisa mostrar diffs enormes.

Resuma as diferenças funcionais.

---

# 12. Recomendar estratégia

Depois de toda a análise, recomende UMA estratégia principal entre:

1. merge;
2. rebase;
3. cherry-pick;
4. outra estratégia segura.

NÃO EXECUTE a estratégia.

Explique:

* por que essa estratégia é a mais segura;
* quais commits devem ser preservados;
* quais commits devem ser integrados;
* ordem recomendada;
* riscos;
* arquivos que provavelmente exigirão resolução manual;
* se recomenda criar uma branch antes da integração.

Se recomendar cherry-pick, informe exatamente quais hashes deveriam ser considerados.

Se recomendar merge, informe qual seria o ponto de partida.

Se recomendar rebase, explique exatamente sobre qual base.

---

# 13. Condição especial

Não presuma que `origin/main` está correto só porque é o remoto.

Também não presuma que os commits locais devem substituir o remoto.

A decisão deve preservar:

1. billing funcionando;
2. Mercado Pago funcionando;
3. trial de 30 dias;
4. preços anuais corretos;
5. booking funcionando;
6. disponibilidade;
7. catálogo global;
8. 20 segmentos;
9. 300 templates;
10. integridade multi-tenant;
11. migrations corretas;
12. testes existentes.

---

# FORMATO FINAL OBRIGATÓRIO

Entregue o resultado nesta estrutura:

## 1. Estado Git

## 2. Merge-base

## 3. Commits locais

## 4. Commits remotos

## 5. Arquivos sobrepostos

## 6. Conflitos reais

## 7. Conflitos potenciais

## 8. Auditoria do billing

## 9. Auditoria do catálogo

## 10. Auditoria das migrations

## 11. Dependências entre commits

## 12. Riscos

## 13. Estratégia recomendada

## 14. Ordem exata da integração recomendada

## 15. O que NÃO deve ser feito

## 16. Veredito

### IMPORTANTE

Não altere absolutamente nada no repositório.

Não faça merge.

Não faça rebase.

Não faça cherry-pick.

Não faça reset.

Não faça push.

Não faça pull.

Não faça fetch.

Não execute migrations.

Somente auditoria e recomendação.
