# PRÉ-INTEGRAÇÃO — SIMULAÇÃO E PLANO DE RESOLUÇÃO

A auditoria Git terminou.

Estado atual:

```text
HEAD local:
18748f7eaa5cf715e0ade18e44c6bd96fcbc3cfe

origin/main:
f6d49436194204ecc11cd27fc832a87a7b0bf919

merge-base:
ed01191f0e03a2d39ab259b57eff43f33e8dde70
```

O HEAD está detached e o working tree está limpo.

A auditoria identificou:

### Commits locais exclusivos

```text
1bf8c2b
1ac8206
2bf7717
0303a99
48135b9
de9cfff
18748f7
```

### Equivalentes no remoto

```text
1bf8c2b ≈ c18f07a
1ac8206 ≈ 479b409
```

### Cadeia de catálogo que precisa ser preservada

```text
0303a99
   ↓
48135b9
   ↓
de9cfff
   ↓
18748f7
```

### Objetivo

Integrar o catálogo local ao `origin/main` preservando também todas as funcionalidades que existem no remoto.

---

# REGRA ABSOLUTA

ESTA ETAPA É SOMENTE DE PLANEJAMENTO.

NÃO ALTERE O REPOSITÓRIO.

NÃO EXECUTE:

* git merge
* git rebase
* git cherry-pick
* git reset
* git checkout
* git switch
* git restore
* git commit
* git push
* git pull
* git fetch

Não altere nenhum arquivo.

Não execute migrations.

Não altere banco remoto.

---

# 1. Simular a aplicação dos commits do catálogo

Quero descobrir exatamente o que aconteceria se partíssemos de:

```text
origin/main
```

e tentássemos incorporar:

```text
0303a99
48135b9
de9cfff
18748f7
```

Faça essa análise usando apenas comandos read-only.

Se necessário, utilize:

```bash
git diff origin/main 0303a99 --stat
git diff origin/main 48135b9 --stat
git diff origin/main de9cfff --stat
git diff origin/main 18748f7 --stat
```

Analise também os diffs dos arquivos críticos.

NÃO faça cherry-pick real.

---

# 2. Analisar cada conflito

Para cada um destes arquivos:

```text
src/lib/booking.functions.ts
src/lib/booking.server.ts
src/lib/subscription.functions.ts
src/routes/$slug.tsx
src/routes/_authenticated/app.assinatura.tsx
src/routes/_authenticated/app.servicos.tsx
```

compare:

```text
origin/main
```

contra:

```text
18748f7
```

e explique exatamente:

### O que existe no remoto

### O que existe no local

### O que precisa ser preservado do remoto

### O que precisa ser preservado do local

### Como unir as duas implementações

Não quero apenas dizer "há conflito".

Quero a orientação funcional para resolver cada conflito.

---

# 3. Atenção especial ao booking

O remoto possui alterações importantes em:

* disponibilidade;
* booking;
* WhatsApp;
* produtos;
* reagendamento;
* trial.

O catálogo local possui alterações em:

* segmentos;
* carregamento de serviços;
* associação de serviços a segmentos;
* agrupamento por segmento;
* catálogo público.

Determine se é possível combinar as implementações sem perder nenhuma funcionalidade.

Para:

```text
booking.server.ts
booking.functions.ts
$slug.tsx
```

explique exatamente quais funções/blocos devem vir de cada lado.

---

# 4. Atenção especial ao subscription

Compare:

```text
origin/main
```

com:

```text
18748f7
```

em:

```text
src/lib/subscription.functions.ts
```

Precisamos preservar simultaneamente:

* Mercado Pago;
* trial de 30 dias;
* preços anuais;
* Basic = 11 meses;
* Medium = 10 meses;
* Unlimited = 9 meses;
* entitlements;
* demais regras existentes no remoto;
* limite de segmentos;
* downgrade seguro.

Não substitua um arquivo pelo outro.

Informe como as duas implementações devem ser combinadas.

---

# 5. Atenção máxima ao billing

Compare:

```text
origin/main
```

com:

```text
18748f7
```

em:

```text
src/lib/billing.server.ts
```

Confirme que a integração deve preservar:

```text
platformSetting
resolveProvider
graceDays
loadSubscriptionByBusiness
loadSubscriptionByProviderId
loadPlanById
businessEntitlements
assertAcceptsBookings
```

Também confirme que nenhuma versão inteira do arquivo deve ser escolhida automaticamente.

Explique se existe alguma diferença funcional ou somente de formatação.

---

# 6. Catálogo

Confirme que a integração precisa preservar integralmente:

```text
src/lib/segments.ts

20260907000000_segments_service_catalog.sql
20260907001000_public_catalog_segments.sql
20260907002000_catalog_structural_fix.sql
20260907003000_complete_global_catalog.sql
20260907004000_catalog_integrity_blockers.sql

tests/segments.test.ts
tests/catalog.test.ts
tests/catalog-integrity.test.ts
```

E que o resultado final contenha:

* 20 segmentos;
* 300 templates;
* exatamente 15 templates por segmento;
* templates globais;
* cópia independente para services;
* sem sincronização template → service;
* `Geral` somente legado/técnico;
* sem fallback automático para `Geral`;
* limites Basic/Medium/Unlimited/Trial;
* downgrade seguro;
* integridade de business_segments;
* integridade de professional_services.

---

# 7. Commits de assinatura

Não planeje aplicar:

```text
1bf8c2b
1ac8206
```

porque já existem equivalentes no remoto:

```text
c18f07a
479b409
```

Verifique, entretanto, se:

```text
2bf7717
```

é necessário.

Confirme se:

```text
tests/subscription-plans.test.ts
```

já existe no estado `origin/main`.

---

# 8. Migrations

Monte a árvore completa das migrations que deverão existir no resultado final.

Separe:

### Migrations remotas

```text
20260906140000_phase2a_instagram.sql
20260906150000_phase0_booking_integrity.sql
20260906170000_phase3_whatsapp_outbox.sql
20260906190000_trial_30_days_all_features.sql
20260906195900_add_rescheduled_status.sql
20260906200000_products_and_reschedule_atomic.sql
```

### Migrations de assinatura

```text
20260906210000_update_annual_plan_discounts.sql
20260906220000_fix_basic_annual_discount.sql
```

### Migrations do catálogo

```text
20260907000000_segments_service_catalog.sql
20260907001000_public_catalog_segments.sql
20260907002000_catalog_structural_fix.sql
20260907003000_complete_global_catalog.sql
20260907004000_catalog_integrity_blockers.sql
```

Verifique se a ordem acima é tecnicamente coerente com as referências reais existentes nos arquivos.

Não execute nenhuma migration.

---

# 9. Verificar risco de duplicação

Confirme se os seguintes pares são funcionalmente equivalentes:

```text
1bf8c2b ↔ c18f07a
1ac8206 ↔ 479b409
```

Se forem equivalentes, eles não devem ser reaplicados.

Também verifique se algum outro commit local já está representado funcionalmente no remoto.

---

# 10. Estratégia de integração

Depois de toda a análise, apresente uma estratégia concreta.

Quero responder às seguintes perguntas:

1. É seguro criar branch a partir de `origin/main`?
2. Devemos integrar `0303a99 → 48135b9 → de9cfff → 18748f7`?
3. Precisamos realmente aplicar `2bf7717`?
4. Existe algum commit intermediário local necessário para o catálogo funcionar?
5. Quais conflitos precisam ser resolvidos manualmente?
6. Qual arquivo é o mais perigoso?
7. Qual migration deve entrar primeiro?
8. Em qual momento devemos rodar testes?
9. Em qual momento podemos criar o commit de integração?
10. Quando será seguro fazer push?

---

# 11. Resultado final obrigatório

Entregue:

## A. Arquitetura final esperada

Mostre graficamente:

```text
origin/main
      +
catálogo local
      =
resultado esperado
```

## B. Commits que DEVEM ser preservados

Liste os hashes.

## C. Commits que NÃO devem ser reaplicados

Liste os hashes.

## D. Conflitos

Tabela:

| Arquivo | Remoto | Local | Estratégia |
| ------- | ------ | ----- | ---------- |

## E. Migrations finais

Lista em ordem.

## F. Riscos

Classifique:

* 🔴 alto
* 🟠 médio
* 🟢 baixo

## G. Sequência exata de integração

Somente comandos/ações que deverão ser executados FUTURAMENTE.

Não execute nenhum deles agora.

## H. Testes obrigatórios após integração

Liste exatamente:

```text
npm test
npx tsc --noEmit
npm run lint
npm run build
git diff --check
```

e quaisquer testes adicionais que considerar necessários.

## I. Veredito

Escolha uma:

🟢 pronto para integração

🟡 integração possível, mas requer resolução manual

🔴 não integrar ainda

---

IMPORTANTE:

Esta tarefa é exclusivamente de análise.

Ao terminar:

```text
git status
```

deve continuar mostrando working tree limpa.

Não altere absolutamente nada.
