Faça uma AUDITORIA TÉCNICA FINAL E SOMENTE LEITURA do commit:

`18748f7eaa5cf715e0ade18e44c6bd96fcbc3cfe`

Mensagem:

`Fix catalog tenant integrity blockers`

Este commit foi criado para corrigir os dois blockers encontrados na auditoria do `de9cfff`:

1. associação de `business_segments` a segmentos globais inativos;
2. ausência de isolamento estrutural entre `professional_services`, `professionals` e `services`.

## REGRA ABSOLUTA

NÃO altere nenhum arquivo.

NÃO crie correções.

NÃO faça novo commit.

NÃO faça push.

NÃO faça merge.

NÃO faça rebase.

NÃO execute migrations no banco remoto.

NÃO altere o banco de produção.

Esta etapa é somente auditoria.

---

# 1. VALIDAR O COMMIT

Confirme:

* HEAD;
* hash completo;
* mensagem;
* commit pai;
* branch;
* detached HEAD;
* working tree;
* arquivos alterados;
* diff exato do commit.

O commit esperado é:

`18748f7eaa5cf715e0ade18e44c6bd96fcbc3cfe`

Pai esperado:

`de9cfff4e0bf342ea03d5a0c98a5f97742611455`

Arquivos esperados:

```text
supabase/migrations/20260907004000_catalog_integrity_blockers.sql
tests/catalog-integrity.test.ts
```

Confirme que nenhum outro arquivo foi alterado.

---

# 2. AUDITAR A MIGRATION

Analise integralmente:

`supabase/migrations/20260907004000_catalog_integrity_blockers.sql`

Verifique:

* BEGIN/COMMIT;
* ausência de operações destrutivas;
* ausência de DELETE;
* ausência de DROP;
* ausência de TRUNCATE;
* ausência de alteração de dados de services;
* ausência de alteração de appointments;
* ausência de alteração de billing;
* ausência de alteração de preços;
* ausência de alteração de duração de serviços reais.

Verifique também:

* funções criadas;
* triggers criados;
* ordem de criação;
* segurança;
* `SECURITY DEFINER`, se utilizado;
* `search_path`;
* possibilidade de SQL injection;
* permissões;
* comportamento em INSERT;
* comportamento em UPDATE.

---

# 3. VALIDAR `business_segments`

Analise diretamente:

`public.validate_business_segment_global()`

e o trigger:

`business_segments_validate_global`

Confirme exatamente:

### Deve rejeitar

* `segment_id` inexistente;
* segmento global inativo;
* tentativa de reativar associação para segmento inativo.

### Deve permitir

* segmento global ativo;
* associação válida respeitando limite do plano;
* associações históricas já existentes mesmo que posteriormente o segmento tenha sido desativado, quando essa for a regra definida pela implementação.

### Deve continuar impedindo

* duplicidade `(business_id, segment_id)`;
* associação indevida entre tenants.

Verifique se a proteção ocorre realmente no PostgreSQL.

Não aceite proteção somente na aplicação.

---

# 4. ANALISAR INTERAÇÃO COM `Geral`

Este ponto é importante.

Confirme o estado real do bucket técnico `Geral`.

Verifique:

* `Geral` continua apenas como legado;
* não aparece como segmento comercial;
* não pode ser selecionado como novo segmento comercial;
* novos serviços não recebem `Geral` automaticamente;
* serviços históricos não são apagados;
* a nova trigger não destrói ou invalida dados legados.

Confirme também que a nova validação não criou uma regressão para serviços antigos.

---

# 5. VALIDAR `professional_services`

Analise integralmente:

`public.validate_professional_service_tenant()`

e:

`professional_services_validate_tenant`

A regra obrigatória é:

```text
professionals.business_id
=
professional_services.business_id

services.business_id
=
professional_services.business_id
```

Teste conceitualmente todos os cenários:

### Deve permitir

```text
Business A
Professional A
Service A
Link A
```

### Deve rejeitar

```text
Business A
Professional A
Service B
```

```text
Business A
Professional B
Service A
```

```text
Business B
Professional A
Service A
```

Também deve rejeitar:

* professional inexistente;
* service inexistente.

---

# 6. PROCURAR BYPASS

Não basta verificar se o trigger funciona.

Procure se existe alguma forma de contornar a validação por:

* INSERT direto;
* UPDATE;
* RPC;
* SECURITY DEFINER;
* função existente;
* endpoint;
* service role;
* operação usada pela aplicação.

Verifique se o trigger cobre tanto:

```text
INSERT
```

quanto:

```text
UPDATE
```

especialmente alterações de:

* `business_id`;
* `professional_id`;
* `service_id`.

---

# 7. RLS

Analise as policies existentes de:

* `business_segments`;
* `professional_services`;
* `professionals`;
* `services`.

Verifique se o RLS e os triggers trabalham juntos corretamente.

Importante:

RLS não deve ser considerado substituto do trigger.

O trigger deve continuar impedindo inconsistências mesmo em chamadas autenticadas que consigam chegar ao INSERT/UPDATE.

---

# 8. SECURITY DEFINER

Se as funções forem `SECURITY DEFINER`, audite especialmente:

* `SET search_path`;
* referências qualificadas com `public.`;
* possibilidade de manipulação de objetos;
* privilégio excessivo;
* possibilidade de um usuário chamar a função diretamente para ultrapassar regras de negócio.

Se não forem `SECURITY DEFINER`, explique por que a função continua funcionando corretamente com RLS.

---

# 9. IDEMPOTÊNCIA

Verifique se a migration pode ser aplicada com segurança dentro do sistema de migrations do Supabase.

Verifique:

* CREATE OR REPLACE;
* DROP TRIGGER IF EXISTS, se necessário;
* DROP FUNCTION IF EXISTS, se necessário;
* conflitos com funções/triggers existentes;
* nomes duplicados;
* dependências.

Não altere a migration.

Apenas reporte riscos.

---

# 10. DADOS EXISTENTES

Como não existe conexão com o banco remoto neste ambiente, NÃO invente resultados.

Separe claramente:

### O que foi comprovado pelo código/migration

de:

### O que ainda precisa ser verificado no banco real.

Identifique quais queries deveriam ser executadas futuramente para detectar:

* `professional_services` cross-tenant;
* `business_segments` apontando para segmentos inexistentes;
* associações para segmentos inativos;
* outras inconsistências relevantes.

NÃO execute essas queries em produção agora.

Apenas forneça as queries de diagnóstico no relatório, se forem úteis.

---

# 11. TESTES

Analise:

`tests/catalog-integrity.test.ts`

Verifique se os testes realmente comprovam o comportamento ou se são apenas testes estáticos de texto.

Classifique cada teste como:

* estrutural;
* estático;
* unitário;
* integração real.

Não considere teste estático equivalente a teste real de PostgreSQL.

---

# 12. REGRESSÃO

Verifique se a correção pode afetar:

* criação de serviço;
* edição de serviço;
* associação profissional;
* remoção de associação;
* disponibilidade;
* booking;
* appointments;
* catálogo público;
* serviços históricos.

Confirme novamente:

`src/lib/billing.server.ts`

não foi alterado.

---

# 13. CATÁLOGO

Confirme que este commit não alterou indevidamente o trabalho já validado:

* 20 segmentos;
* 300 templates;
* 15 por segmento;
* nomes;
* durações;
* cópia independente;
* templates globais;
* `Geral`.

---

# 14. VALIDAÇÕES LOCAIS

Execute:

```powershell
npm test
npx tsc --noEmit
npm run lint
npm run build
git diff --check
```

Reporte exatamente os resultados.

---

# 15. GIT — NÃO ALTERAR

Apenas inspecione:

```powershell
git status
git log --oneline --decorate -20
git rev-parse HEAD
git rev-parse origin/main
git merge-base HEAD origin/main
git log --oneline HEAD..origin/main
git log --oneline origin/main..HEAD
```

NÃO faça:

* merge;
* rebase;
* reset;
* cherry-pick;
* push;
* force-push.

Queremos apenas saber exatamente como está a divergência.

---

# 16. VEREDITO

Classifique:

### 🟢 APROVADO

Se os dois blockers estiverem estruturalmente corrigidos e não houver outro problema relevante.

### 🟡 APROVADO COM RESSALVAS

Somente se houver apenas pontos de validação futura sem risco técnico imediato.

### 🔴 NÃO APROVADO

Se existir qualquer falha estrutural, de segurança, isolamento ou regressão.

---

# FORMATO FINAL

## AUDITORIA — 18748f7

### 1. Status

### 2. Commit

### 3. Migration

### 4. `business_segments`

### 5. `professional_services`

### 6. RLS

### 7. SECURITY DEFINER

### 8. Geral

### 9. Dados existentes

Separar comprovado de não comprovado.

### 10. Testes

Tabela:

| Validação        | Resultado |
| ---------------- | --------- |
| npm test         |           |
| TypeScript       |           |
| lint             |           |
| build            |           |
| git diff --check |           |

### 11. Regressão

### 12. Git

### 13. BLOCKERS

### 14. WARNINGS

### 15. VEREDITO FINAL

🟢 / 🟡 / 🔴

NÃO faça nenhuma alteração durante a auditoria.
